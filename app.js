var React = require('react'),
    ReactDOM = require('react-dom'),
    MainComponent = require('./js/components/main.jsx');

ReactDOM.render(
    <div>
        <MainComponent />
    </div>,
    document.getElementById('content-wrapper')
);
